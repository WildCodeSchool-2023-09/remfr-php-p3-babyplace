{% extends 'base.html.twig' %}

{% block title %}EmergencyContact{% endblock %}

{% block body %}
    <h1>EmergencyContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emergency_contact.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ emergency_contact.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ emergency_contact.lastname }}</td>
            </tr>
            <tr>
                <th>PhoneContact</th>
                <td>{{ emergency_contact.phoneContact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emergency_contact_index') }}">back to list</a>

    <a href="{{ path('app_emergency_contact_edit', {'id': emergency_contact.id}) }}">edit</a>

    {{ include('emergency_contact/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('child_delete', {'family_id': app.user.family.id, 'child_id': child.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn">Delete</button>
</form>
